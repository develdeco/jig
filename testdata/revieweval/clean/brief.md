# Brief: string helpers

Add a `stringutil` package with `Reverse(s string) string` (returns s with
its runes reversed) and `TrimAndLower(s string) string` (trims surrounding
whitespace and lowercases the result). No known issues; this diff is
correct as written.
