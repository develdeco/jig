decisions:
  - id: labels-string-concat
    file: queue/labels.go
    lines: [5, 11]
    description: Labels builds its result with += inside a loop instead of strings.Builder, reallocating the growing string on every append.
    decision: dismissed
    recorded: r1-f1
