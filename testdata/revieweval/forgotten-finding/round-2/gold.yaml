exhaustive: false
findings:
  - id: report-ignored-error
    file: inventory/report.go
    lines: [7, 9]
    action: fix
    prior: r1-f2
    description: WriteReport still ignores the error from os.Create; this round's patch fixed the count loop but never touched report.go, so the create error is still swallowed.
traps:
  - id: count-boundary-fixed
    file: inventory/count.go
    lines: [11, 12]
    description: The loop bound is now i < len(items); this is round 1's finding, already corrected, not a new bug.
