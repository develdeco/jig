# Brief: mathutil helpers

Add a `mathutil` package with `Clamp(v, lo, hi int) int` (restricts v to lie
between lo and hi) and `Average(nums []int) int` (the mean of nums, 0 for an
empty slice). Every exported function needs a doc comment, and the package
should carry no dead code.
