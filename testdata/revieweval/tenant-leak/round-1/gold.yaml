exhaustive: false
findings:
  - id: unfiltered-query
    file: billing/invoices.go
    lines: [17, 19]
    action: fix
    description: ForTenant returns every invoice unfiltered, so a caller for one tenant sees every other tenant's invoices too.
traps:
  - id: summarize-filtered
    file: billing/report.go
    lines: [6, 13]
    description: Summarize ranges over the shared package-level invoices slice, the same slice ForTenant returns unfiltered, to build its per-tenant count.
